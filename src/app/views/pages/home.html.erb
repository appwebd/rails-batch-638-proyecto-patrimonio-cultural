<div class="container">
  <div class="row">
    <div class="col-sm-4 col-md-4 col-lg-4">
      <br>
      <h1>A dónde vas <br class="mobile-br"> a viajar?</h1><br>
      <p class="text-justify"> Chile al alcance de todos. <br>
        Conoce el capital cultural de la mano de sus personajes y de impresionantes paisajes.</p>
      <br>
      <p><strong>Ven a conocernos</strong></p>
      <br>

      <%= simple_form_for @patrimonio, url: resultado_path, html: {novalidate: true} do |form| %>


        <%= form.input :ubicacion, id: 'patrimonio_ubicacion', required:true, class:"form-control", type: "search" %>

        <%= form.submit 'Buscar', class: "btn btn-primary button_rounded" %>
      <% end %>

    </div>

    <div class="col-sm-8 col-md-8">
      
      <div id="map"
           class="mapStyle"
           data-markers="<%= @markers.to_json %>"
           data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>


  </div>

</div>
